/** Source selection tiers — routing only; no automatic optimisation. */
export const SOURCE_TIERS = {
  PRIMARY: "primary",
  SECONDARY: "secondary",
  INACTIVE: "inactive",
  RECOMMENDATION: "recommendation",
};

export const DEFAULT_SOURCE_PRIORITY = 100;
